${userImport}

class ${domainClassName} {

    int uid
    String screenName

    String tokenSecret
    String token

	  static belongsTo = [${userConnectionProperty}: ${userClassName}]

	  static constraints = {
		  uid unique: true
	  }
}
